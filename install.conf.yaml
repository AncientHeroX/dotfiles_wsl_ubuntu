- defaults:
    link:
      relink: true

- clean: ['~']

- link:
     ~/.zshrc: 
     ~/.gitconfig:
#    ~/.vim: vim
#    ~/.vimrc: vimrc

#- create:
#    - ~/downloads
#    - ~/.vim/undo-history

# - shell:
#   - [git submodule update --init --recursive, Installing submodules]
