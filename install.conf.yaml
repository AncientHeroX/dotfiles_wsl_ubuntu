- defaults:
    link:
      relink: true

- clean: ['~']

- link:
     ~/.zshrc: 
     ~/.gitconfig:
     ~/.poshthemes:

#- create:
#    - ~/downloads
#    - ~/.vim/undo-history

- shell:
   - [git submodule update --init --recursive, Installing submodules]
